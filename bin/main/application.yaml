server:
  port: 8888
  tomcat:
   connection-timeout: 2s
   threads:
    max: 50
    min-spare: 5

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sriramvivekk/cloud_test_config_repo
          default-label: main